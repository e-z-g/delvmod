
A script in subindex 13, believed to have something to do with displaying papers with aspect >= 2. (papers with aspect < 2, but not 0xff, are handled by 0E64) It is called from 1086.Use with the parameters A2=0x11, A3=0x27, A4=0x34, A5=0x000000BC, A6=0x00000095 

It uses gui_create with parameters (prop, A2=0x11, A3=0x27, A4=0x34, A5=0x000000BC, A6=0x00000095) 



```
function (theObject,messageText,A2,A3,A4,A5,A6) (theWindow)
  ifn 
    opDB
      arg thePaper
    end
  then L000E

  return
    byte 0
  end
 
  L000E:
  set theWindow
    gui_create 
      arg thePaper
      arg A2
      arg A3
      arg A4
      arg A5
      arg A6
    end
  end

  gui_0x07
    local theWindow
    arg messageText
    byte 0
    byte 0
    A5
    A6
  end

  return
    byte 0
  end
```
